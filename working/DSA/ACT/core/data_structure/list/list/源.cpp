#include<iostream>
#include<list>
#include<vector>
using namespace std;
#include"list.h"





int main()
{
	xdh::testX();

	/*list<int> l;
	cout << l.front();*/
	/*std:list<int> L1;
	std::vector<int> v = { 1,2,3,4,5 };

	list<int>::iterator it = L1.insert(L1.begin(), v.begin(), v.end());
	xdh::print_container(L1);
	 it = L1.insert(L1.begin(), v.begin(), v.end());
	xdh::print_container(L1);

	L1.erase(L1.begin(), L1. begin());
	xdh::print_container(L1);*/

	return 0;
}
